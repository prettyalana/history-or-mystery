<%= turbo_stream_from "room_#{@room.code}_seat_#{Current.player.seat}" %>

<div data-controller="resync">
  <div id="room-content">
    <%= render "rooms/gameplay", room: @room, viewer: Current.player %>
  </div>
</div>